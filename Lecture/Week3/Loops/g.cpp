// Loops: for, while and do while

#include <iostream>

using namespace std;

int main() {
    
    // while loop that repeats 10 times
    int i = 0; // counter
    while(i < 10) {
        cout << i << endl;
        ++i; // incrementing the counter at the end of each iteration
    }

    return 0;
}