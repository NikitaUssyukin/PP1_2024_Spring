// Loops: for, while and do while

#include <iostream>

using namespace std;

int main() {
    int a;
    cin >> a;

    int base = a;

    a *= base; // same as a = a * base
    a *= base;
    a *= base;

    cout << a;

    return 0;
}