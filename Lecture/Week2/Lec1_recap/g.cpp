#include <iostream>
#include <cmath>

using namespace std;

int main() {

    int a, b;

    cin >> a >> b;
    
    cout << pow(a, b) << endl; // a to the power of b
    cout << sqrt(a) << endl;
    cout << abs(a) << endl;

    // https://www.w3schools.com/cpp/cpp_math.asp
    
    return 0;
}